#!/bin/sh

title="Configuring Neomutt"
dialog --title "$title" --yesno "Do you want to configure Neomutt?" 0 0

if [ $? == 0 ]; then
	mkdir -p $HOME/.config/mutt/accounts/
	configs=(colour.muttrc gpg.rc mailcap)
	for config in ${configs[@]}; do
		cp $config $HOME/.config/mutt/$config
	done
fi
